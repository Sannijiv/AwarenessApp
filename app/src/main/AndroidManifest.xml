<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanjiv.awarenessapp">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        >

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCFaVDFGPJwfJKvJuUJmWn0v5RODL4pWYg"/>

        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration" />
        <activity android:name=".ResetPage" />
        <activity android:name=".LampAdapter" />
        <activity android:name=".LampDetails" />
        <activity android:name=".AddLamp" />
        <activity android:name=".EditLamp" />
        <activity android:name=".AddNotifyLamp" />

        <service
            android:name=".NotificationService"
            android:exported="false" />

        <activity
            android:name=".DeleteNotifyLamp"
            android:label="@string/title_activity_delete_notify_lamp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PiDetails" />
        <activity android:name=".PiEdit" />
        <activity android:name=".MapActivity"></activity>



    </application>

</manifest>